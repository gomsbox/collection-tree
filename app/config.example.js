/* 로컬 개발용 템플릿. 이 파일을 config.js로 복사한 뒤 SHEET_API_URL을 채워서 쓴다.
 * (config.js는 실제 백엔드 URL이 담기므로 .gitignore에 의해 저장소에는 올라가지 않는다.
 *  GitHub Pages 배포본은 Actions 시크릿(SHEET_API_URL)으로 빌드 시점에 자동 생성된다.)
 */
window.CT_CONFIG = {
  SHEET_API_URL: "",
};
